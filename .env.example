# AI Services — optional. The app works without these;
# AI features will degrade gracefully when keys are absent.

# Google Gemini (primary) — free tier: 1,500 req/day, no credit card needed
# Get key: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY=

# Groq (fallback) — free tier
# Get key: https://console.groq.com/keys
VITE_GROQ_API_KEY=
